/**
 * Callback method when form is (re)loaded.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"BEB9549C-2C45-4662-9BBC-384ABC5C98D5"}
 */
function onLoad(event) {
	initDesktop(event);
}
